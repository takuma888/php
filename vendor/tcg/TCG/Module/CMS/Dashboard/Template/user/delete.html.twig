<form class="tui modal-content" method="post" action="{{ url('dashboard.user.delete', { ids: ids }) }}">
    <div class="modal-header">
        <h5 class="modal-title" id="role-edit-modal">
            <i class="fa fa-remove"></i>&nbsp;删除后台用户
        </h5>
        <button type="button" class="close" data-dismiss="layer" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body">
        <div class="alert alert-danger" role="alert">
            确认删除以下后台用户吗？并不是删除数据，而是解除这些角色在后台的角色
        </div>
        <table class="table table-sm table-hover table-striped table-bordered">
            <thead>
            <tr>
                <th>ID</th>
                <th>用户名</th>
                <th>名称</th>
                <th>创建</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>#{{ user.id }}</td>
                    <td>{{ user.username }}</td>
                    <td>{{ user.name }}</td>
                    <td>{{ user.create_at|date('Y-m-d H:i:s') }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-primary btn-sm">提交</button>
        <button type="button" class="btn btn-secondary btn-sm" data-dismiss="layer">取消</button>
    </div>
</form>
